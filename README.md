# cs566

1. Make sure image files are located in the correct folder
2. Make sure CLIP and other dependencies are installed
3. Run makeimage_replicate.py to get image embeddings
4. Move to R to analyze embeddings
5. Run make_cossim_replication.R to calculate within-group cosine similarity and create the analytical datasets
6. Run make_analysis_replication.R to generate and save all regression tables and figures
